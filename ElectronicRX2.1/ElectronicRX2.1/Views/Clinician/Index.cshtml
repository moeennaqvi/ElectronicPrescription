@model ElectronicRX2._1.Models.ClinicianSearchViewModel
@{
    ViewBag.Title = "View";
 }

<script src="~/Scripts/jquery-1.10.2.js"></script>
<script src="~/Scripts/jquery.unobtrusive-ajax.js"></script>

<h2>Search Clinic Admins</h2>
<hr />

@using (Ajax.BeginForm("Search", "Clinician", new AjaxOptions()
                                                        {
                                                            HttpMethod = "POST",
                                                            UpdateTargetId = "divUsers",
                                                            InsertionMode = InsertionMode.Replace
                                                        }))
{
    <div class="row">
        @Html.Label("Search By")
        @Html.DropDownListFor(m => m.condition, (SelectList)ViewBag.SearchType)
        @Html.TextBoxFor(m => m.value)
        <input type="submit" value="Search" class="btn btn-sm btn-primary" />
    </div>
    <hr />


}

<div id="divUsers">

</div>

<p>
    <a href="~/Account/Register" class="btn btn-primary">Create New</a>
</p>